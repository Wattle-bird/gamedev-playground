import * as PIXI from "pixi.js";
import { Engine } from "../engine";
import { AbstractScene } from "./abstractScene";

export class TEMPLATEScene extends AbstractScene {
  constructor(e: Engine) {
    super(e)
  }

  update(dt: number) {
  }

}
